# Use a slim base image for a lightweight container
FROM nginx:alpine

# Copy static HTML to the appropriate location
COPY index.html /usr/share/nginx/html/index.html

# Expose the port explicitly for clarity
EXPOSE 80

